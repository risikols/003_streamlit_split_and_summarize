[tool.poetry]
name = "ai-long-text-summarizer"
version = "0.1.0"
description = "Summarize long texts using OpenAI and LangChain"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "ai_summarizer"}]

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.30.0"
pandas = "^2.1.0"
langchain = "^0.1.0"  # Ajusta a la versión estable actual
openai = "^1.32.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


